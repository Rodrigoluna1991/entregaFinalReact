import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

// Crear un contenedor raíz y renderizar la app
const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
